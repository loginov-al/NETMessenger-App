# Скопировать на сервер:
#   sudo mkdir -p /etc/netmessenger
#   sudo cp deploy/web.env.example /etc/netmessenger/web.env
#   sudo chmod 640 /etc/netmessenger/web.env
#   sudo chown root:www-data /etc/netmessenger/web.env

# CDN Timeweb (SSL на стороне Timeweb Cloud)
CDN_BASE=https://4opwz855t4.cdn.twcstorage.ru

# Gunicorn
GUNICORN_BIND=127.0.0.1:8000
GUNICORN_WORKERS=3
LOG_LEVEL=info

# Flask
FLASK_ENV=production
